"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyQueryString = applyQueryString;
exports.toUpperCaseKeys = toUpperCaseKeys;
exports.hasSuffInfo = hasSuffInfo;
const utils_1 = require("@zcatalyst/utils");
function applyQueryString(baseUrl, queryStrings) {
    const keys = Object.keys(queryStrings);
    for (let i = 0; i < keys.length; i++) {
        const key = keys[i];
        const value = encodeURIComponent(queryStrings[key]);
        baseUrl += `${i === 0 ? '?' : '&'}${key}=${value}`;
    }
    return baseUrl;
}
function toUpperCaseKeys(objectToConvert) {
    const obj = objectToConvert;
    const keys = Object.keys(obj);
    const len = keys.length;
    const convertedObj = {};
    for (let i = 0; i < len; i++) {
        convertedObj[keys[i].toUpperCase()] = obj[keys[i]];
    }
    return convertedObj;
}
function hasSuffInfo(target, matches, autoHandleException = true) {
    (0, utils_1.isNonEmptyObject)(target);
    for (const match of matches)
        if (!(match in target)) {
            if (autoHandleException)
                return false;
        }
    return true;
}
